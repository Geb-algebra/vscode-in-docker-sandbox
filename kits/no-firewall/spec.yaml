schemaVersion: "2"
kind: mixin
name: vscode-in-docker-sandbox-no-firewall
displayName: Unrestricted outbound network access
description: Allows all outbound network traffic for a sandbox.

permissions:
  network:
    allow:
      - "**"
